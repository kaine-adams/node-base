block content
    if !user
        div
            a(href='/login') Login
        div
            a(href='/register') New User
    else
        div Welcome #{user.username}
        div 
            a(href='/game') Proceed to Game
